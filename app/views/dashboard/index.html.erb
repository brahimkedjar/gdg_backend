<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>

  <!-- Inline CSS -->
  <style>
    .dashboard-container {
      padding: 40px;
      text-align: center;
      font-family: Arial, sans-serif;
    }

    .dashboard-container h1 {
      font-size: 2rem;
      margin-bottom: 20px;
    }

    .dashboard-container a {
      display: inline-block;
      margin: 10px;
      padding: 15px 25px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      font-size: 1.2rem;
    }

    .dashboard-container a:hover {
      background-color: #0056b3;
    }

    .btn-danger {
      background-color: #dc3545;
      color: white;
      padding: 10px 20px;
      border-radius: 5px;
      font-size: 1.2rem;
    }

    .btn-danger:hover {
      background-color: #c82333;
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <h1>Admin Dashboard</h1>
    <p>Select a section to manage:</p>
    
    <!-- Links to View Individuals and Teams -->
    <a href="<%= admin_individuals_path %>" class="btn btn-primary mb-3">View Individuals</a>
    <a href="<%= admin_teams_path %>" class="btn btn-primary mb-3">View Teams</a>
    
    <br>
  </div>
</body>
</html>
